# `@okikio/html`

HTML traversal and extraction primitives built around a small runtime-neutral document model.

## Use

```ts
import * as html from '@okikio/html';
```

The package models HTML structure and generic extraction. Network acquisition, browser execution, and domain-specific interpretation stay outside this package.
