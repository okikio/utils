# `@okikio/version`

Version parsing and comparison helpers for software-version strings encountered in metadata and detection systems.

## Use

```ts
import * as version from '@okikio/version';
```

The package compares version representations. Release policy, package publication, and product compatibility decisions belong to callers.
