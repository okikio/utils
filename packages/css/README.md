# `@okikio/css`

Small CSS parsing and resource-classification primitives for code that needs structured stylesheet information without owning a browser runtime.

## Use

```ts
import * as css from '@okikio/css';
```

The package operates on CSS inputs and generic resource kinds. Fetching, browser capture, persistence, and product-specific classification remain caller responsibilities.
