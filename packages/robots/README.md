# @okikio/robots

Network-free robots.txt records and parse-once/check-many matching. HTTP status, redirect, cache, authority, and sitemap-fetch policy remain in `the consuming discovery package`.

The matcher is not called Google-parity until the upstream `google/robotstxt` corpus is run against it.
