# `@okikio/hash`

Deterministic hashing helpers for byte and text inputs.

## Use

```ts
import * as hash from '@okikio/hash';
```

Use this package for representation-level hashing. Domain identifiers, persistence keys, and collision policy remain owned by the consuming package.
