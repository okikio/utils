/**
 * Explicit resource-management compatibility entrypoints.
 *
 * Runtime code uses the ECMAScript disposal globals directly. Import
 * `@okikio/dispose/polyfill` only from environments that do not provide them.
 *
 * @module
 */
export {};
